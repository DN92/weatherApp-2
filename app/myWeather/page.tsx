import { Paper, TextInput, Button, Text, Group, Select } from "@mantine/core"
import { useState, useEffect} from 'react'

interface inputProps {
  lon: string,
  lat: string,
}

const MyWeather = ({lon, lat}: inputProps) => {
  return (
    <div>

    </div>
  )
}

export default MyWeather
